name: Run with Jaeger (host network)

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  run-jaeger:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Start Jaeger (host network)
        run: |
          docker run -d --name jaeger \
            --network host \
            -e COLLECTOR_ZIPKIN_HTTP_PORT=9411 \
            cr.jaegertracing.io/jaegertracing/all-in-one:1.74.0

          echo "Jaeger started on host network — UI at http://localhost:16686"

      - name: Wait for Jaeger to be ready
        run: |
          echo "Waiting for Jaeger to start..."
          for i in {1..10}; do
            if curl -s http://localhost:16686 > /dev/null; then
              echo "Jaeger is ready!"
              exit 0
            fi
            echo "Still waiting..."
            sleep 3
          done
          echo "Jaeger did not start in time" && exit 1

      - name: Run tests (example)
        run: |
          echo "Running tests while Jaeger is active..."
          # npr. mvn test ili pytest

      - name: Stop and remove Jaeger
        if: always()
        run: |
          echo "Stopping Jaeger..."
          docker stop jaeger || true
          docker rm jaeger || true
